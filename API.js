// you can just copy and paste the API
// note replace the whatever with whatever you want :|
function ActivateBrain() {
    // What these scripts do is when submit is pressed then it will respond.
    if (whatever = "hello") {
        AIoutput.innerText = "Hello how are you"; // Sorry if im doing DRY
    }
    if (whatever == "Hello") {
        AIoutput.innerText = "Hello how are you";
    }
    if (whatever === "Hi") {
        AIoutput.innerText = "Hi how are you";
    }
    if (whatever === "Whats 2+2") {
        document.write("2 + 2");
    }
}
